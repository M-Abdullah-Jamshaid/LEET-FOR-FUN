/* Write your PL/SQL query statement below */
SELECT (select name from department d where d.id=e.departmentId)"Department" ,e.name AS "Employee",e.salary as"Salary" from Employee e
where e.salary=any(Select max(salary)over(partition by departmentId) from Employee f where e.departmentId =f.departmentId  )
